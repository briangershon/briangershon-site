<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} - {% endif %}{{ site.title | default: "Brian Gershon" }}</title>
    <meta name="description" content="{% if description %}{{ description }}{% else %}{{ site.description | default: "Personal blog of Brian Gershon - thoughts on technology, software development, and life." }}{% endif %}">
    <meta name="author" content="{{ site.author.name | default: "Brian Gershon" }}">
    <meta name="robots" content="index, follow">
    <meta name="theme-color" content="#2563eb">
    
    <!-- Canonical URL -->
    <link rel="canonical" href="{{ site.url }}{{ page.url }}">
    
    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="{% if title %}{{ title }} - {% endif %}{{ site.title | default: "Brian Gershon" }}">
    <meta property="og:description" content="{% if description %}{{ description }}{% else %}{{ site.description | default: "Personal blog of Brian Gershon - thoughts on technology, software development, and life." }}{% endif %}">
    <meta property="og:type" content="{% if date %}article{% else %}website{% endif %}">
    <meta property="og:url" content="{{ site.url }}{{ page.url }}">
    <meta property="og:site_name" content="{{ site.title | default: "Brian Gershon" }}">
    <meta property="og:image" content="{{ site.url }}/assets/images/og-image.svg">
    <meta property="og:image:alt" content="{% if title %}{{ title }}{% else %}{{ site.title | default: "Brian Gershon" }}{% endif %}">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    {% if date %}
    <meta property="article:author" content="{{ site.author.name | default: "Brian Gershon" }}">
    <meta property="article:published_time" content="{{ date | date: "%Y-%m-%dT%H:%M:%S%z" }}">
    {% if tags %}{% for tag in tags %}<meta property="article:tag" content="{{ tag }}">
    {% endfor %}{% endif %}
    {% endif %}
    
    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:site" content="@brianfive">
    <meta name="twitter:creator" content="@brianfive">
    <meta name="twitter:title" content="{% if title %}{{ title }} - {% endif %}{{ site.title | default: "Brian Gershon" }}">
    <meta name="twitter:description" content="{% if description %}{{ description }}{% else %}{{ site.description | default: "Personal blog of Brian Gershon - thoughts on technology, software development, and life." }}{% endif %}">
    <meta name="twitter:image" content="{{ site.url }}/assets/images/og-image.svg">
    <meta name="twitter:image:alt" content="{% if title %}{{ title }}{% else %}{{ site.title | default: "Brian Gershon" }}{% endif %}">
    
    <!-- RSS Feeds -->
    <link rel="alternate" type="application/rss+xml" title="{{ site.title | default: "Brian Gershon" }} Atom Feed" href="/feed.xml">
    <link rel="alternate" type="application/rss+xml" title="{{ site.title | default: "Brian Gershon" }} RSS Feed" href="/feed.rss">
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    
    <!-- Stylesheets -->
    <link rel="stylesheet" href="/assets/css/style.css">
    
    <!-- JSON-LD Structured Data -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "{% if date %}BlogPosting{% else %}WebSite{% endif %}",
        {% if date %}
        "headline": "{{ title }}",
        "description": "{% if description %}{{ description }}{% else %}{{ site.description }}{% endif %}",
        "author": {
            "@type": "Person",
            "name": "{{ site.author.name }}",
            "url": "{{ site.url }}"
        },
        "publisher": {
            "@type": "Person",
            "name": "{{ site.author.name }}",
            "url": "{{ site.url }}"
        },
        "datePublished": "{{ date | date: "%Y-%m-%dT%H:%M:%S%z" }}",
        "dateModified": "{{ date | date: "%Y-%m-%dT%H:%M:%S%z" }}",
        "mainEntityOfPage": {
            "@type": "WebPage",
            "@id": "{{ site.url }}{{ page.url }}"
        },
        "image": "{{ site.url }}/assets/images/og-image.svg",
        "url": "{{ site.url }}{{ page.url }}"
        {% else %}
        "name": "{{ site.title }}",
        "description": "{{ site.description }}",
        "url": "{{ site.url }}",
        "author": {
            "@type": "Person",
            "name": "{{ site.author.name }}",
            "url": "{{ site.url }}",
            "sameAs": [
                "{{ site.github }}",
                "{{ site.twitter }}"
            ]
        },
        "publisher": {
            "@type": "Person",
            "name": "{{ site.author.name }}"
        }
        {% endif %}
    }
    </script>
    
    <!-- Fathom Analytics -->
    {% if site.fathom.site_id %}
    <script src="https://cdn.usefathom.com/script.js" data-site="{{ site.fathom.site_id }}" defer></script>
    {% endif %}
</head>
<body class="min-h-screen flex flex-col">
{% include "header.liquid" %}


    <main class="flex-1">
        {{ content }}
    </main>

    <footer class="bg-gray-50 border-t border-gray-200 mt-12">
        <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            <div class="flex flex-col md:flex-row justify-between items-center">
                <div class="text-gray-600 text-sm mb-4 md:mb-0">
                    © {{ "now" | date: "%Y" }} {{ site.title | default: "Brian Gershon" }}.
                </div>
                <div class="flex space-x-6">
                    <a href="/feed.xml" class="text-gray-600 hover:text-gray-900 transition-colors text-sm">
                        Feed (Atom)
                    </a>
                    <a href="/feed.rss" class="text-gray-600 hover:text-gray-900 transition-colors text-sm">
                        Feed (RSS)
                    </a>
                    {% for social in site.social %}
                    <a href="{{ social.href }}" class="text-gray-600 hover:text-gray-900 transition-colors text-sm" target="_blank" rel="noopener noreferrer" {% if social.trackEvent %}onclick="window.fathom.trackGoal('{{ social.trackEvent }}', 0);"{% endif %}>
                        {{ social.name }}
                    </a>
                    {% endfor %}
                </div>
            </div>
        </div>
    </footer>
</body>
</html>